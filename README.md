# munkres
implementation of the hungarian algorithm in c with a python wrapper

An implementation of the hungarian algorithm with a python wrapper. The basic 
computation part is implemented in C in order to be as fast as possible.

Usage
=====

Build the module by running make on the command line

 make
 
See the test_munkres_psalign.py for example usage. The example itself requires 
the BioPython project but should be sufficient for explaining how the module
should be used.

